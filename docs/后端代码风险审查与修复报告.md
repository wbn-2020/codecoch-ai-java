# 后端代码风险审查与修复报告

审查日期：2026-05-19
仓库：`<LOCAL_WORKSPACE>\CodeCoachAI-java`
分支：`dev-v3`
审查对象：当前后端工作区代码、Nacos 配置、SQL migration、后端文档。

## 任务范围声明

1. 对应 V3 PRD 与 V3-A 规划范围：以岗位目标 / JD 驱动闭环为主线，覆盖 V3-B 已实现的 Target Job / JD 基础能力，并复核已提前进入 V3-D 至 V3-G 的匹配、能力画像、任务、通知、搜索和日志能力。
2. 优先级：P0/P1 风险收敛优先，P2 作为阶段治理风险记录。
3. 当前 V3 阶段：`dev-v3` 后端分支，主线仍应按 V3-B 到 V3-H 分阶段验收。
4. 影响模块：`codecoachai-user`、`codecoachai-question`、`codecoachai-resume`、`codecoachai-interview`、`codecoachai-ai`、`codecoachai-task`、`codecoachai-search`、`codecoachai-gateway`、`codecoachai-common`、`codecoachai-system`、`codecoachai-file`。
5. 影响表：`target_job`、`job_description_analysis`、`resume_job_match_report`、`resume_job_match_detail`、`skill_profile`、`skill_gap_item`、`study_plan_skill_relation`、`question_recommendation`、`async_task`、`notification`、`search_index_record`、`operation_log`、`login_log`、`sys_announcement` 等 V3 表。
6. 影响 API：`/job-targets/**`、`/resume-job-match/**`、`/skill-profiles/**`、`/question-recommendations/**`、`/study-plans/**`、`/study-tasks/**`、`/study-checkins/**`、`/notifications/**`、`/search/**`、`/admin/**`、`/inner/**`。
7. 前端契约影响：有。Gateway 路由、用户侧搜索隔离、目标岗位/JD/匹配/学习计划/通知/搜索接口均会影响前端联调。
8. 是否需要新模块：否。本轮只复核和收敛现有模块。
9. 是否需要 SQL migration：本轮未新增 migration，但执行了现有 V3 migration 重复运行验证。
10. 验证方法：静态扫描、`mvn clean compile`、敏感信息扫描、V3 migration 重复执行、关键表和索引核验、`git diff --check`。

## 总体结论

当前后端代码已恢复到可编译状态，早期报告中记录的 P0 编译阻断、Gateway V3 路由缺失、`/inner/**` 新服务白名单缺失、搜索用户越权、搜索同步占位文档、异步题目生成假成功、V3_012 migration 非幂等写法等高风险点，复核结果均已收敛。

本轮实际补充修复了两类风险：清理 README 中的本地 MySQL 明文密码示例，以及归档过期的早期风险报告，避免后续继续引用已经失效的错误结论。

## 风险复核结果

| 编号 | 原风险 | 当前状态 | 复核结论 |
| --- | --- | --- | --- |
| R-01 | 根工程编译失败 | 已修复 | `mvn clean compile` 全模块通过。 |
| R-02 | Gateway 缺失 V3 路由 | 已修复 | `config/nacos/codecoachai-gateway-dev.yml` 已覆盖 job-targets、resume-job-match、question-recommendations、study-tasks、study-checkins、admin/search、admin/roles 等路径。 |
| R-03 | task/search 被内部调用白名单拦截 | 已修复 | `InternalCallFilter` 已包含 `codecoachai-task` 与 `codecoachai-search`，仍保留 HMAC、timestamp、nonce replay protection。 |
| R-04 | 用户侧搜索越权 | 已修复 | `/search/resumes` 与 `/search/interviews` 均从登录态获取 userId；管理员搜索拆在 `/admin/search/**` 并做 `requireAdmin()`。 |
| R-05 | 搜索同步写入占位文档 | 已修复 | ES UPSERT 拉取文档为空或失败时抛错重试，不写空占位文档。 |
| R-06 | 异步题目生成假成功 | 已修复 | 消费者调用 question-service 内部落库接口，保存数量匹配后才标记成功和发送通知。 |
| R-07 | V3 JD 解析未接入 AI 调用增强日志 | 已部分收敛 | `JOB_DESCRIPTION_PARSE` 已通过 `AiCallLogService` 调用并写入 route trace/token/cost 日志；其他历史 AI 场景仍保留原日志路径，后续可逐步统一。 |
| R-08 | V3_012 migration 幂等性风险 | 已修复并验证 | V3_012 已使用 helper procedure；V3 全量 migration 在本地库重复执行通过。 |
| R-09 | 文档/示例中残留明文数据库密码 | 已修复 | README 示例改为临时环境变量；全仓库未再扫描到该明文密码。 |
| R-10 | V3 阶段范围跨度大 | 已记录 | 这是阶段治理风险，不是当前编译/安全阻断。建议后续按 V3-B/V3-D/V3-F/V3-G 分批验收。 |
| R-11 | docs/nacos 与 config/nacos 分叉 | 已收敛到可用状态 | 默认导入源已转到 `docs/nacos/`；`config/nacos/` 仅保留为历史/手工模板参考，双目录并存仍有维护漂移风险。 |

## 本轮修复

1. `README.md`
   - 移除本地 MySQL 示例命令中的明文密码。
   - 改为使用临时 `MYSQL_PWD` 环境变量执行命令，执行后清理环境变量。

2. `docs/dev-v3后端代码风险审查报告.md`
   - 将早期快照报告归档。
   - 明确当前有效结论以本报告为准，避免过期 P0/P1 结论误导联调。

3. `docs/后端代码风险审查与修复报告.md`
   - 新增当前审查、修复、验证和剩余风险记录。

## 验证记录

| 验证项 | 命令/方式 | 结果 |
| --- | --- | --- |
| 当前分支和工作区 | `git status --short --branch` | 位于 `dev-v3`。 |
| 全模块编译 | `mvn clean compile` | 通过，23 个 Maven reactor 模块全部 SUCCESS。 |
| V3 migration 重复执行 | 对 `sql/migration/V3_*.sql` 按文件名顺序执行两轮 | 通过，无重复列、重复索引或重复表错误。 |
| 关键 V3 表核验 | information_schema 查询关键表 | `target_job`、`job_description_analysis`、`resume_job_match_report`、`skill_profile`、`async_task`、`notification`、`search_index_record` 等存在。 |
| 关键索引核验 | `SHOW INDEX` / information_schema | `idx_target_job_current`、`idx_jd_analysis_target_job`、`idx_question_normalized_title`、`idx_qr_source` 等存在。 |
| 敏感信息扫描 | `rg` 扫描明文数据库密码和危险 password 参数 | 未发现残留明文密码。 |

## API 影响

本轮未新增或删除 API。当前复核确认 Gateway 已覆盖 V3 关键用户侧、管理侧和搜索侧路径；`/inner/**` 仍为内部调用路径，不应暴露给前端。

## 数据库影响

本轮未新增 SQL 文件，未修改表结构。验证过程中在本地 `codecoachai` 数据库重复执行了既有 V3 migration，并确认重复执行成功。该操作用于验证幂等性，未做破坏性变更。

## 前端影响

前端可继续按现有 V3 路由联调。用户侧搜索接口不接受用户传入的 userId，必须依赖登录态；管理员全量搜索应使用 `/admin/search/**`。

## 剩余风险

1. 未执行完整运行时集成测试，因此 Nacos 注册、Gateway 实际转发、Sa-Token 登录态、Redis、RocketMQ、Elasticsearch、OSS 等仍需在联调环境验证。
2. AI 调用增强日志已覆盖 `JOB_DESCRIPTION_PARSE`，但历史 AI 场景仍存在直接 `aiClient.chat` 路径，建议后续按场景逐步迁移，不建议在一次修复中大范围改动稳定 V1/V2 链路。
3. V3 功能范围已经超过 V3-B，后续应拆阶段验收，避免把搜索、任务、通知、日志、题目增强与 Target Job/JD 基础验收混在一个通过标准里。

## 下一步建议

先进入 V3-B/V3-D 主链路联调：目标岗位创建、设置当前目标、JD 解析、简历-JD 匹配、匹配报告查询。该链路通过后，再单独验收 V3-F 能力画像/学习计划联动和 V3-G 任务、通知、搜索工程能力。
